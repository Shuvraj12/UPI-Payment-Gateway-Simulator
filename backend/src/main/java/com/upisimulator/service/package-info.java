/**
 * Business logic, as interfaces plus {@code impl} implementations.
 * <p>
 * Empty in Phase 1. The first pair ({@code AuthService} / {@code AuthServiceImpl})
 * arrives in Phase 2. Controllers depend on the interface, never the
 * implementation, so services stay swappable and mockable in tests.
 */
package com.upisimulator.service;
