/**
 * Business logic, as interfaces plus {@code impl} implementations.
 * <p>
 * Phase 2 adds the first pair: {@link com.upisimulator.service.AuthService} /
 * {@code impl.AuthServiceImpl}. Controllers depend on the interface, never
 * the implementation, so services stay swappable and mockable in tests.
 */
package com.upisimulator.service;
